<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Dashboard Capteurs</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 2rem; background: #f7f7f7; }
        h1 { color: #2c3e50; }
        table { border-collapse: collapse; width: 100%; margin-bottom: 2rem; }
        th, td { border: 1px solid #ccc; padding: 8px 12px; text-align: center; }
        th { background: #eaeaea; }
    </style>
</head>
<body>
    <h1>Dashboard des capteurs</h1>

    <h2>Période analysée</h2>
    <p>{{ startDate }} &nbsp;→&nbsp; {{ endDate }}</p>
    <p>Durée : {{ duration }} — Nombre de mesures : {{ readingsCount }}</p>

    <h2>Dernière lecture</h2>
    {% if lastReading %}
        <table>
            <thead>
                <tr>
                    {% for k in lastReading.keys %}<th>{{ k }}</th>{% endfor %}
                </tr>
            </thead>
            <tbody>
                <tr>
                    {% for v in lastReading.values %}<td>{{ v }}</td>{% endfor %}
                </tr>
            </tbody>
        </table>
    {% else %}
        <p>Aucune donnée disponible.</p>
    {% endif %}

    <h2>Statistiques</h2>
    <table>
        <thead>
            <tr>
                <th>Capteur</th><th>Min</th><th>Max</th><th>Moyenne</th><th>Écart-type</th>
            </tr>
        </thead>
        <tbody>
            {% for sensor, s in stats %}
                <tr>
                    <td>{{ sensor }}</td>
                    <td>{{ s.min }}</td>
                    <td>{{ s.max }}</td>
                    <td>{{ s.avg }}</td>
                    <td>{{ s.stddev }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html> 