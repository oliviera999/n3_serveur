# Composer
/vendor/
composer.phar

# Environnement (secrets)
.env
.env.local

# Logs
error_log
cronlog.txt
cronlog*.txt

# Fichiers temporaires / exports
*.csv
/tmp/

# IDE / OS
.DS_Store
.idea/
.vscode/
desktop.ini

# Cache
/var/cache/

# Fichier de test / outil local (ne pas versionner)
/var/last_device_ip_test.txt

# Archives (exclues du depot — conservees localement, lecture seule)
/archives/
# Extrait FFP3 pour analyse (lecture seule, contient des references historiques de la cle API)
/analyse-ffp3/
# Patch / explorations frontend (non versionne)
/ameliorations-visuelles-iot-serveur/

# Dossier source des photos (build-input local) : les images finales sont
# versionnees dans public/assets/images/aquaponie-description/. Eviter le doublon.
/photos aquaponie/

# PHP / PHPUnit / PHPStan / PHP-CS-Fixer
.phpunit.result.cache
.phpunit.cache/
.php-cs-fixer.cache
.phpstan.cache/

# Photos uploadees (garder la structure via .gitkeep)
/uploads/msp1/*
!/uploads/msp1/.gitkeep
/uploads/n3pp/*
!/uploads/n3pp/.gitkeep
/uploads/ffp3/*
!/uploads/ffp3/.gitkeep
/uploads/trash/
# Dossiers de test ad-hoc (photos locales smoke test, etc.)
/uploads/test-*/

# Rate limiting cache (genere par GalleryUploadController)
/var/cache/ratelimit/


# Agent worktrees (Claude Code, transient)
/.claude/
