{# Panneau « État du système » — utilisé par les pages de données MSP1, N3PP. #}
<div class="system-health-panel">
    <h4 class="system-health-panel-title"><i class="fas fa-heartbeat"></i> {{ health_panel_title|default('État du système') }}</h4>
    <div class="health-items">
        {% block health_items %}{% endblock %}
    </div>
</div>
