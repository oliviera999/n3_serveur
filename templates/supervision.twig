{% extends 'layout.twig' %}
{# n³ iot datas - Page de supervision - Liens vers toutes les pages du projet #}

{% block title %}{{ page_title|default('Supervision') }} - n³ iot datas{% endblock %}

{% block wrapper_class %}supervision-wrapper{% endblock %}

{% block head_css %}
<link rel="stylesheet" href="{{ base_path|default('') }}/assets/css/supervision-styles.css" />
{% endblock %}

{% block content %}
<article class="post featured">
    <header class="major" data-aos="fade-up">
        <p class="version-badge">Version du site {{ version|default('–') }}</p>
        <h1><i class="fas fa-cog"></i> Supervision</h1>
        <p>Accès central à toutes les pages du projet n³ IoT (dashboards, contrôle, administration).</p>
        <hr />
    </header>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-broadcast-tower"></i>
        <h2>Live</h2>
    </div>
    <div class="live-section" data-aos="fade-up">
        <p class="live-section-intro">Témoins de connexion et dernière réception de données par module — mise à jour en temps réel</p>
        <p class="live-section-legend"><strong>Légende :</strong> WROOM et S3 = types de cartes électroniques (ESP32) ; MSP1 = potager (station météo) ; N3PP = serre (élevage d'insectes)</p>
        <div class="live-grid" id="live-status-grid" aria-live="polite">
            <a href="{{ base_path|default('') }}/aquaponie" class="live-card live-loading" data-module="ffp3-wroom-prod"><div class="live-card-name">Aquaponie (WROOM prod)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/aquaponie-test" class="live-card live-loading" data-module="ffp3-wroom-test"><div class="live-card-name">Aquaponie (WROOM test)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/aquamobile" class="live-card live-loading" data-module="ffp3-s3-prod"><div class="live-card-name">Aquamobile (S3 prod)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/aquamobile-test" class="live-card live-loading" data-module="ffp3-s3-test"><div class="live-card-name">Aquamobile (S3 test3)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/meteo" class="live-card live-loading" data-module="msp1-prod"><div class="live-card-name">Potager (MSP1 prod)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/msp1-test/msp1datas/msp1-data.php" class="live-card live-loading" data-module="msp1-test"><div class="live-card-name">Potager (MSP1 test)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/serre" class="live-card live-loading" data-module="n3pp-prod"><div class="live-card-name">Élevage (N3PP prod)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
            <a href="{{ base_path|default('') }}/n3pp-test/n3ppdatas/n3pp-data.php" class="live-card live-loading" data-module="n3pp-test"><div class="live-card-name">Élevage (N3PP test)</div><span class="live-card-badge loading"><i class="fas fa-spinner fa-spin"></i> Chargement...</span><div class="live-card-datetime">—</div></a>
        </div>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-home"></i>
        <h2>Accueil & projet</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <a href="{{ base_path|default('') }}/" class="link-card"><i class="fas fa-home"></i><span class="label">Accueil</span><div class="hint">Page d'accueil du portail</div></a>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-fish"></i>
        <h2>Aquaponie FFP3 (WROOM)</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <a href="{{ base_path|default('') }}/aquaponie" class="link-card"><i class="fas fa-chart-area"></i><span class="label">Vue principale (prod)</span><div class="hint">Vue paysage</div></a>
        <a href="{{ base_path|default('') }}/aquaponie-alt" class="link-card"><i class="fas fa-chart-line"></i><span class="label">Vue classique (prod)</span><div class="hint">Vue alternative</div></a>
        <a href="{{ base_path|default('') }}/aquaponie-control" class="link-card"><i class="fas fa-sliders-h"></i><span class="label">Contrôle (prod)</span><div class="hint">Pompes, chauffage, lumière</div></a>
        <a href="{{ base_path|default('') }}/dashboard" class="link-card"><i class="fas fa-tachometer-alt"></i><span class="label">Dashboard (prod)</span><div class="hint">Capteurs FFP3</div></a>
        <a href="{{ base_path|default('') }}/tide-stats" class="link-card"><i class="fas fa-water"></i><span class="label">Statistiques marées (prod)</span><div class="hint">Analyse des marées</div></a>
        <a href="{{ base_path|default('') }}/aquaponie-control-test" class="link-card"><i class="fas fa-vial"></i><span class="label">Contrôle (test)</span><div class="hint">Environnement test</div></a>
        <a href="{{ base_path|default('') }}/dashboard-test" class="link-card"><i class="fas fa-tachometer-alt"></i><span class="label">Dashboard (test)</span><div class="hint">Environnement test</div></a>
        <a href="{{ base_path|default('') }}/tide-stats-test" class="link-card"><i class="fas fa-water"></i><span class="label">Statistiques marées (test)</span><div class="hint">Environnement test</div></a>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-mobile-alt"></i>
        <h2>Aquamobile (S3)</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <a href="{{ base_path|default('') }}/aquamobile" class="link-card"><i class="fas fa-chart-area"></i><span class="label">Vue principale (prod S3)</span><div class="hint">Vue paysage – board S3</div></a>
        <a href="{{ base_path|default('') }}/aquamobile-alt" class="link-card"><i class="fas fa-chart-line"></i><span class="label">Vue classique (prod S3)</span><div class="hint">Vue alternative – board S3</div></a>
        <a href="{{ base_path|default('') }}/aquamobile-control" class="link-card"><i class="fas fa-sliders-h"></i><span class="label">Contrôle (prod S3)</span><div class="hint">Board S3</div></a>
        <a href="{{ base_path|default('') }}/dashboard3" class="link-card"><i class="fas fa-tachometer-alt"></i><span class="label">Dashboard (prod S3)</span><div class="hint">Capteurs board S3</div></a>
        <a href="{{ base_path|default('') }}/tide-stats3" class="link-card"><i class="fas fa-water"></i><span class="label">Statistiques marées (prod S3)</span><div class="hint">Analyse des marées S3</div></a>
        <a href="{{ base_path|default('') }}/aquamobile-test" class="link-card"><i class="fas fa-vial"></i><span class="label">Vue principale (test3)</span><div class="hint">Environnement test3</div></a>
        <a href="{{ base_path|default('') }}/aquamobile-alt-test" class="link-card"><i class="fas fa-vial"></i><span class="label">Vue classique (test3)</span><div class="hint">Environnement test3</div></a>
        <a href="{{ base_path|default('') }}/aquamobile-control-test" class="link-card"><i class="fas fa-vial"></i><span class="label">Contrôle (test3)</span><div class="hint">Environnement test3</div></a>
        <a href="{{ base_path|default('') }}/dashboard3-test" class="link-card"><i class="fas fa-tachometer-alt"></i><span class="label">Dashboard (test3)</span><div class="hint">Environnement test3</div></a>
        <a href="{{ base_path|default('') }}/tide-stats3-test" class="link-card"><i class="fas fa-water"></i><span class="label">Statistiques marées (test3)</span><div class="hint">Environnement test3</div></a>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-seedling"></i>
        <h2>Potager (MSP1)</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <a href="{{ base_path|default('') }}/meteo" class="link-card"><i class="fas fa-chart-line"></i><span class="label">Données</span><div class="hint">Station météo / potager (prod)</div></a>
        <a href="{{ base_path|default('') }}/meteo-control" class="link-card"><i class="fas fa-sliders-h"></i><span class="label">Contrôle</span><div class="hint">Sorties station météo (prod)</div></a>
        <a href="{{ base_path|default('') }}/msp1-test/msp1datas/msp1-data.php" class="link-card"><i class="fas fa-vial"></i><span class="label">Données (test)</span><div class="hint">Environnement test</div></a>
        <a href="{{ base_path|default('') }}/msp1-test/msp1control/" class="link-card"><i class="fas fa-vial"></i><span class="label">Contrôle (test)</span><div class="hint">Environnement test</div></a>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-bug"></i>
        <h2>Élevage (N3PP)</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <a href="{{ base_path|default('') }}/serre" class="link-card"><i class="fas fa-chart-line"></i><span class="label">Données</span><div class="hint">Serre / élevage (prod)</div></a>
        <a href="{{ base_path|default('') }}/serre-control" class="link-card"><i class="fas fa-sliders-h"></i><span class="label">Contrôle</span><div class="hint">Sorties serre (prod)</div></a>
        <a href="{{ base_path|default('') }}/n3pp-test/n3ppdatas/n3pp-data.php" class="link-card"><i class="fas fa-vial"></i><span class="label">Données (test)</span><div class="hint">Environnement test</div></a>
        <a href="{{ base_path|default('') }}/n3pp-test/n3ppcontrol/" class="link-card"><i class="fas fa-vial"></i><span class="label">Contrôle (test)</span><div class="hint">Environnement test</div></a>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-images"></i>
        <h2>Galeries photo (ESP32-CAM)</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <a href="{{ base_path|default('') }}/gallery/msp1" class="link-card"><i class="fas fa-camera"></i><span class="label">Photos du potager (MSP1)</span><div class="hint">Galerie photos station météo</div></a>
        <a href="{{ base_path|default('') }}/gallery/n3pp" class="link-card"><i class="fas fa-camera"></i><span class="label">Photos de l'élevage (N3PP)</span><div class="hint">Galerie photos N3PP</div></a>
        <a href="{{ base_path|default('') }}/gallery/ffp3" class="link-card"><i class="fas fa-camera"></i><span class="label">Photos aquaponie (FFP3)</span><div class="hint">Galerie photos FFP3</div></a>
    </div>

    <div class="section-header" data-aos="fade-up">
        <i class="fas fa-tools"></i>
        <h2>Administration</h2>
    </div>
    <div class="links-grid" data-aos="fade-up">
        <div class="link-card cache-card">
            <div><i class="fas fa-broom"></i><span class="label">Vider tous les caches du site</span></div>
            <div class="hint">Twig, DI, OpCache PHP, cache outputs (prod, test, S3, test3)</div>
            <button type="button" id="clearAllCacheBtn" class="btn-cache" data-clear-url="{{ base_path }}/admin/clear-cache">Vider tous les caches</button>
            <div id="clearCacheResult" class="cache-result" aria-live="polite"></div>
        </div>
        <a href="{{ base_path }}/admin/deploy-script" class="link-card">
            <div><i class="fas fa-rocket"></i><span class="label">Script de déploiement</span></div>
            <div class="hint">Voir DEPLOY_NOW.sh – à exécuter sur le serveur (SSH) après git pull</div>
        </a>
    </div>
</article>
{% endblock %}

{% block footer %}
{% include 'partials/_footer.twig' with {'footer_text': 'N3 IoT Datas – Supervision'} %}
{% endblock %}

{% block scripts_vendor %}
<script src="{{ base_path }}/assets/js/jquery.min.js"></script>
<script src="{{ base_path }}/assets/js/jquery.scrollex.min.js"></script>
<script src="{{ base_path }}/assets/js/jquery.scrolly.min.js"></script>
<script src="{{ base_path }}/assets/js/browser.min.js"></script>
<script src="{{ base_path }}/assets/js/breakpoints.min.js"></script>
<script src="{{ base_path }}/assets/js/util.js"></script>
<script src="{{ base_path }}/assets/js/main.js"></script>
{% endblock %}

{% block scripts %}
<script>
(function() {
    var BASE = '{{ base_path }}' || '';
    var LIVE_MODULES = [
        { id: 'ffp3-wroom-prod', url: BASE + '/api/realtime/system/health' },
        { id: 'ffp3-wroom-test', url: BASE + '/api/realtime-test/system/health' },
        { id: 'ffp3-s3-prod', url: BASE + '/api/realtime3/system/health' },
        { id: 'ffp3-s3-test', url: BASE + '/api/realtime3-test/system/health' },
        { id: 'msp1-prod', url: BASE + '/msp1/api/realtime/system/health' },
        { id: 'msp1-test', url: BASE + '/msp1-test/api/realtime/system/health' },
        { id: 'n3pp-prod', url: BASE + '/n3pp/api/realtime/system/health' },
        { id: 'n3pp-test', url: BASE + '/n3pp-test/api/realtime/system/health' }
    ];
    var POLL_INTERVAL_MS = 15000;

    function formatDatetime(str) {
        if (!str) return '—';
        var d = new Date(str.replace(' ', 'T'));
        if (isNaN(d.getTime())) return str;
        var pad = function(n) { return (n < 10 ? '0' : '') + n; };
        return pad(d.getDate()) + '/' + pad(d.getMonth() + 1) + '/' + d.getFullYear() + ' ' + pad(d.getHours()) + ':' + pad(d.getMinutes()) + ':' + pad(d.getSeconds());
    }

    function updateCard(moduleId, health) {
        var card = document.querySelector('.live-card[data-module="' + moduleId + '"]');
        if (!card) return;
        var badge = card.querySelector('.live-card-badge');
        var datetimeEl = card.querySelector('.live-card-datetime');
        if (!badge || !datetimeEl) return;

        if (health === null) {
            card.className = 'live-card live-error';
            badge.className = 'live-card-badge error';
            badge.innerHTML = '<i class="fas fa-exclamation-triangle"></i> Erreur';
            datetimeEl.textContent = '—';
            return;
        }

        var online = health.online === true;
        card.className = 'live-card ' + (online ? 'live-online' : 'live-offline');
        badge.className = 'live-card-badge ' + (online ? 'online' : 'offline');
        badge.innerHTML = '<i class="fas fa-circle"></i> ' + (online ? 'LIVE' : 'HORS LIGNE');
        datetimeEl.textContent = formatDatetime(health.last_reading || null) || '—';
    }

    function setLoading(moduleId) {
        var card = document.querySelector('.live-card[data-module="' + moduleId + '"]');
        if (!card) return;
        var badge = card.querySelector('.live-card-badge');
        var datetimeEl = card.querySelector('.live-card-datetime');
        if (!badge || !datetimeEl) return;
        card.className = 'live-card live-loading';
        badge.className = 'live-card-badge loading';
        badge.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Chargement...';
        datetimeEl.textContent = '—';
    }

    function fetchOne(mod) {
        return fetch(mod.url).then(function(r) {
            if (r.ok) return r.json();
            return null;
        }).catch(function() { return null; });
    }

    function pollAll(isFirst) {
        if (isFirst) {
            LIVE_MODULES.forEach(function(mod) { setLoading(mod.id); });
        }
        Promise.all(LIVE_MODULES.map(function(mod) {
            return fetchOne(mod).then(function(h) { updateCard(mod.id, h); });
        })).catch(function() {});
    }

    pollAll(true);
    setInterval(function() { pollAll(false); }, POLL_INTERVAL_MS);
})();

document.getElementById('clearAllCacheBtn').addEventListener('click', async function () {
    var btn = this;
    var resultEl = document.getElementById('clearCacheResult');
    var url = btn.getAttribute('data-clear-url');
    btn.disabled = true;
    resultEl.className = 'cache-result';
    resultEl.textContent = 'Vidage en cours…';
    resultEl.style.display = 'block';
    try {
        var response = await fetch(url);
        var data = await response.json();
        resultEl.className = 'cache-result ' + (data.success ? 'success' : 'error');
        resultEl.textContent = data.message || (data.errors && data.errors.join(' ; ')) || 'Erreur inconnue';
    } catch (e) {
        resultEl.className = 'cache-result error';
        resultEl.textContent = 'Erreur : ' + (e.message || 'réseau');
    } finally {
        btn.disabled = false;
    }
});
</script>
{% endblock %}
